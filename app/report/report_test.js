'use strict';

describe('secMapApp.report module', function() {

  beforeEach(module('secMapApp.report'));

  describe('list controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var reportCtrl = $controller('Report2Ctrl');
      expect(reportCtrl).toBeDefined();
    }));

  });
});